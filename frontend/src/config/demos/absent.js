// Substitui a configuração da outra vertical nos builds fixados em um tenant.
// O bundle publicado não deve conter nome, textos, cores nem rota do demo que
// não é dele — nem para exibir, nem para o leitor curioso do JavaScript.
export default null;
