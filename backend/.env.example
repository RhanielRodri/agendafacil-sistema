DATABASE_URL="postgresql://postgres:postgres@localhost:5432/agendafacil?schema=public"
PORT=4000
FRONTEND_URL="http://localhost:5173"
NODE_ENV="development"
# Credenciais administrativas locais — apenas nomes aqui, nunca valores reais.
# Defina valores só no .env local (ignorado) ou no painel do provedor.
STUDIO_CUT_ADMIN_EMAIL=
STUDIO_CUT_ADMIN_PASSWORD=
LUMIERE_ADMIN_EMAIL=
LUMIERE_ADMIN_PASSWORD=
